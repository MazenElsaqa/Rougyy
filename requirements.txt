# --- Phase 0/1: core, DB, config, tracing ---
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Structured tracing (OpenTelemetry), introduced in Phase 1 per spec
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24

# Testing
pytest>=8.0
pytest-cov>=5.0

# --- Added in later phases (kept commented until needed) ---
# sqlglot>=25.0            # Phase 4: AST-based SQL validation
# google-genai>=0.5        # Phase 5: Gemini structured output / function calling
# chromadb>=0.5            # Phase 11/12: vector store for RAG
# fastapi>=0.110           # Phase 23: backend API
# uvicorn[standard]>=0.29  # Phase 23: ASGI server
# sse-starlette>=2.1       # Phase 23: SSE streaming endpoint
# streamlit>=1.32          # Phase 22: internal debug tool

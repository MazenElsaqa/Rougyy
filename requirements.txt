# --- Phase 0/1: core, DB, config, tracing ---
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Structured tracing (OpenTelemetry), introduced in Phase 1 per spec
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24

# Testing
pytest>=8.0
pytest-cov>=5.0

# --- Milestone 1: thin end-to-end skeleton (generate -> validate -> execute -> answer) ---
sqlglot>=25.0              # AST-based SQL validation (folds Phase 4 into Milestone 1 as a safety layer)
openai>=1.30               # LLM integration (OpenAI-compatible SDK — works with GPT-4o, Gemini, or any provider)

# --- Added in later milestones (kept commented until needed) ---
# chromadb>=0.5            # Milestone 4+: vector store for schema RAG (only once schema size justifies it)
fastapi>=0.110             # Milestone 6: backend API
uvicorn[standard]>=0.29    # Milestone 6: ASGI server
# sse-starlette>=2.1       # Future: SSE streaming endpoint
# streamlit>=1.32          # Phase 22: internal debug tool
